fn sum(a, c) {
    let total = 0;
    for v in a {
        total += v;
    }
    c.send(total);
}

let a = [7, 2, 8, -9, 4, 0];
let n = len(a) / 2;
let c = chan();

go sum(a[:n], c);
go sum(a[n:], c);
let x = c.recv();
let y = c.recv();
assert([x, y] == [-5, 17]);
